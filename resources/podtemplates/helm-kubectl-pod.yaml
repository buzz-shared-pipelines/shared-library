kind: Pod
metadata:
  name: helm-kubectl
spec:
  containers:
  - name: helm-kubectl
    image: dtzar/helm-kubectl:2.14.3
    resources:
      requests:
        memory: "1Gi"
    command:
    - cat
    tty: true
